# TODO: May need to overhaul build backend to move 'com/autogenerated files into a separate 'build' directory
#  A fair bit of work, so that should be done later on...

[project]
name = "SaveSync (CST8002)"
version = "0.5"
authors = [
    {name = "Tehgan Porter", email = "tehgan@proton.me"},
]
description = "SaveSync (CST8002 prototype)"

[tool.pyside6-project]
files = ["src/helpers/url_helper.py", "src/main.py", "src/parser.py", "src/ui/Preferences.qml", "src/ui/main.qml"]
